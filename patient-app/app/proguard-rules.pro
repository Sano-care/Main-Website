# Phase 0: minification disabled in debug builds — Phase 6 wires the
# release signing config + R8 minification with explicit keep rules for
# Kotlinx Serialization data classes and Retrofit service interfaces.
